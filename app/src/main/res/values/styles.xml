<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">#20b2aa</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="custom_color">#676972</item>
    </style>

    <style name="SwitchTheme1" parent="@android:style/Theme.Black">
        <item name="custom_color">#FF00FF00</item>
        <!-- 如果string类型不是填的引用而是直接放一个字符串,在布局文件中使用正常,但代码里获取的就有问题 -->
    </style>

    <style name="SwitchTheme2" parent="@android:style/Theme.Wallpaper">
        <item name="custom_color">#FFFFFF00</item>
    </style>

</resources>
